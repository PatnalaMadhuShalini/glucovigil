# Database Configuration
# For local development (PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/glucovigil?sslmode=prefer

# For Supabase deployment
# Format: postgresql://postgres:password@db.project-ref.supabase.co:5432/postgres?sslmode=require
# DATABASE_URL=postgresql://postgres:your_password@db.your_project_ref.supabase.co:5432/postgres?sslmode=require

# Session Configuration 
# Generate a strong secret with: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
SESSION_SECRET=your_strong_session_secret_here

# Server Configuration
PORT=5000
NODE_ENV=development

# For production deployment on Render, Railway, Heroku, etc.
# NODE_ENV=production
# PORT=10000 (Render uses this internal port)